# Notebooks

`failure_analysis.ipynb` — Day 5. Load `results/*_records.jsonl`, categorise
errors by hand, cross-reference `data/degradation_manifest.json` to see which
corruption drove which error type.

Keep exploratory work here; anything that needs to be reproducible belongs in
`src/`.
